// =-=-=-=-=-=-=-=Ideias=-=-=-=-=-=-=-=



//  Usar uma biblioteca random pra sortear as pontuações da vitória

// Fazer um futsal, menos jogadores e mais fácil de organizar tendo 2 alas, 2 fixos e goleiro

// Timeout

// Classe principal de gerenciamento de time

// Classe principal (pt2) para o gerenciamento de pessoas (associação)

// C/ Herança vai tar as classes

// Jogador (atributos:
// Ala,
// Fixo,
// Goleiro)
// Partida atributos:
//  time1,
// time2,
// time3,
// balancear partidas: métodos
// {
//    2 ala para cada time, 2 fixos para cada            time, goleiro
// }

//-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=--=--=-=-=--=-=-=-=-=--=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-


//the first thing to do:

//make the class players, im think add CRUD sistem
//- when add player he need: years old, name and position

//port pro alisson :>
// fazer com que os jogadores estejam no banco de dados salvos em associação e  tambem com RA

// sistema de crud pode ficar fora da classe jogadores

// Gestão de Jogos:
// CRUD dos Jogos;
// Os atributos principais são: a data do jogo, o local, o tipo de campo e quantos jogadores por time incluindo o goleiro;
// Adicionar um atributo opcional que limita a quantidade de times e/ou de jogadores;
// A quantidade mínima para confirmar a partida são pelo menos dois times completos.
// Lista de Interessados para registrar quem pretende ir ao próximo jogo








//=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-//=-=-=-=-=-=-=-=-==-=-=-=-=-=-=-//=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=//

27/5
to com duvida pra finalizar times, fiquei incomodao com o foreach finalizar amanhã

- o sistema de justiça vai priorizar quem for o primeiro interessado, e goleiros
tendo isso em mente quero limitar 2 goleiros por jogo ou 3 por time, penso em fechar os times com 4 jogadores, e depois verificar e adicionar goleiro(se necessario) caso não tiver os ultimos da lista jogarão nessa posição

segue a baixo algumas implementação para times:

para os goleiros pensei em algo tipo 
var goleiro = jogadores.FirstOrDefault(j => j.Posicao == "Goleiro" && Interessados.Contains(j.RA));
if (goleiro != null)
{
    PrimeiroTime.Add(goleiro.nome);
    Interessados.Remove(goleiro.RA); +--------+> Remove o goleiro da lista de interessados
}

de inicio e ia implementar isso aqui mas achei que ficou ruim, vou deixar anotado aqi para caso eu ter ideia mais tarde

if (limitador == 0){
            PrimeiroTime?.Add(jogador.nome);
 *       else if (timeAtual == 1)
 *           SegundoTime?.Add(jogador.nome);
  *      else
  *          TerceiroTime?.Add(jogador.nome);}
